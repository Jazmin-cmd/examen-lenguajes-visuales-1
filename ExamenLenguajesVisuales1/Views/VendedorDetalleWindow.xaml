<Window x:Class="ExamenLenguajesVisuales1.Views.VendedorDetalleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Detalle Vendedor" Height="250" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <Label Content="Nombre:"/>
            <TextBox x:Name="txtNombre"/>

            <Label Content="Teléfono:" Margin="0,10,0,0"/>
            <TextBox x:Name="txtTelefono"/>

            <Label Content="Email:" Margin="0,10,0,0"/>
            <TextBox x:Name="txtEmail"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Guardar" Width="75" Margin="5" Click="Guardar_Click"/>
            <Button Content="Cancelar" Width="75" Margin="5" Click="Cancelar_Click"/>
        </StackPanel>
    </Grid>
</Window>

